# Distinct units — 2026-09-12

Baseline: clean HEAD 9b6b1b707f11792559e5cd5f39cd29b61ac9b836.
Audit: four regions / twelve dynamically duplicated encounters; eight legacy encounter
resources, six swords, typed directed elemental cycle and four exact effect colors.
FighterView always uses samurai attack/support sheets; nearest filtering already set.
Existing Aseprite Lua Sprite/Image generators and independent compressed-cel/PNG
verifiers provide the pipeline. Export uses an explicit resource allowlist, excludes
sources/tests, and gates a 1 MiB PCK. Existing suite: 19,840 checks, including 72
starting-build campaigns and exported legacy/area full clears. Preserve all assertions.

Bounded execution (two planned passes; at most four total):
1. Author twelve 48x48 four-frame/100ms attack sets exclusively using Aseprite APIs.
   Each gets unique geometry, weapon, palette, editable source, JSON, PNG and
   SpriteFrames. Record all baseline art hashes; independent read-only verifier
   checks compressed native cels, exported pixels, alpha, timing, palette, geometry
   differences, metadata and hashes. Gate: unit verifier + full verify.sh.
2. Add data-only UnitSpec and twelve area EncounterSpec resources; preserve legacy
   encounters and all combat values. Select units in FighterView, show roles in
   map/HUD/intro/loot and boss markers/scale. Test all mappings, normal and exported
   resource selection, complete campaign, preservation; full verify.sh (includes
   Web export and exported full clears). Native 390x844 smoke/capture if display
   available. Update README/CREDITS/dated QA/STOP_REASON, diff check, local commit.

No push/deploy. Python may inspect bytes and write text metadata, never author raster.

Pass 1 complete: Aseprite authored 12 sets; independent verifier passed 48 poses,
60 hashes, 66 geometry pairs. Baseline art preservation passed. Full verify.sh:
19,840 checks / 0 failures, exported eight-encounter and twelve-node/four-boss clears.
Initial Aseprite default config write failed; local XDG_CONFIG_HOME resolved it.

Pass 2 complete: dedicated UnitSpec + 12 area resources, enemy selection, role
labels, normal/exported full clears. Targeted and full gates: 20,226 / 0.
Web PCK 195,740 bytes / 237 entries. X11/Wayland unavailable; Xvfb cannot bind.
All 12 source strips visually inspected through the image viewer.

Pass 3 (final bounded polish): long map labels need explicit line separation to
keep role text readable across every region. Add all-region layout checks,
Aseprite-native read-only verification and reproducible PNG check; document final
results. Gate targeted suite + full verify.sh again. No gameplay scope change.

Pass 3 complete: explicit three-line map unit labels and all-region layout checks.
Targeted + full gates each 20,286 checks / 0 failures. Native Aseprite verification
and fresh twelve-PNG reproduction passed. Final export: 237 entries, PCK 195,756
bytes; both campaigns cleared in exported pack. All 91 prior art hashes unchanged.
Native display/Xvfb blocked (zero screenshots), all 12 sprite strips inspected.
README, CREDITS, ASSET_SPEC, dated QA and STOP_REASON updated. Diff check passed.
Local commit failed with read-only .git/index.lock (128); HEAD remains 9b6b1b7.
Verified unstaged work left for parent workspace. No push/deployment. Complete;
remaining browser/device/native-render acceptance is explicitly documented in QA.
