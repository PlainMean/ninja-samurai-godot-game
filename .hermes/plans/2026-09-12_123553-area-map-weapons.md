# Area map / weapons implementation

Audit: clean HEAD cce61af, eight Resource encounters; RunModel linear intro/shrine/reveal states; CombatModel fixed 1/2 elemental damage and typed effects; four touch actions; portrait layout and native smoke. All Aseprite sheets and frame resources remain untouched. Export is an explicit resource allowlist, single-threaded Web/Compatibility. Existing regression suites and asset verifiers will remain.

1. Model/data pass: add immutable weapon specs, four areas of three nodes, forked normal-node order then boss, seeded RNG and bounded techniques. Retain eight-seal API as reusable legacy campaign. Add model tests and run full verify gate.
2. UI pass: default area campaign, starting sword/pair choices, tappable area/node map, loot/inventory and shrine techniques; retain journey/reveal and pause. Add scene and exported campaign coverage; full verify gate.
3. QA/documentation pass: complete deterministic path checks, native screenshot inspection if display available, README/roadmap/QA/STOP_REASON, full verify, diff check and local commit if allowed.

Maximum three planned cohesive passes (never more than five). Each pass must pass targeted tests and full game/tests/verify.sh before the next. No push/deploy. RNG uses explicit integer Park–Miller state owned by RunModel; roll = next state modulo 4 + 1, matching bonus = 1 + roll modulo 2; technique contribution = level - 1 (levels 1–3); matchup +1 only on the exact directed cycle. Forecast gives inclusive range without consuming state.

Execution: all three planned passes completed. Pass 1 gate: 14,267 checks;
pass 2 gate: 14,280 checks with both exported campaigns. Pass 3 adds 72 complete
starting-build/seed/route campaigns, exhaustive additive matrix and UI regression
coverage. Final QA records exact gate totals. Native display and Xvfb attempts
failed before rendering; no screenshots claimed. No fourth feature pass started.
