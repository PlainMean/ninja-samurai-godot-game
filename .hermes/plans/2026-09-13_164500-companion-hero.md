# Companion hero — 2026-09-13
Base: clean e71a0a2. No push or deployment.

Inspected: four-area ring map (Fire entry, two guards per boss), twelve UnitSpec silhouettes/intent cycles, legacy eight encounters, RunModel/CombatModel and event boundary ordering, fighter/arena/duel/HUD/modal/area-panel scenes, manifests, native decoders, test runner and resource export allowlist.

## Bounded passes (maximum four)
1. Author Kira using Aseprite Lua only; editable 48px four-frame 100ms source/export, typed HeroSpec, independent decoder/hash/palette verification. Gate: Aseprite generator/native verifier, independent verifier, existing full verify.sh.
2. Integrate deterministic model recruitment, combat, shrine development and projection UI; retain existing assertions while explicitly acknowledging new join state in clear drivers. Add targeted companion tests. Gate: targeted suite and verify.sh.
3. Inspect sprite and attempt 390x844 native captures, export-pack resource/full-clear coverage, polish any observed issues and documentation. Gate: native/independent asset checks, targeted suite, verify.sh (Web export and pack smoke), diff check.
4. Reserved only for a concrete failed gate; same targeted/full gates if needed.

## Rules
Kira the Tideblade: WATER tide warden, braided hair, crescent poleblade, teal/ivory armor. Six HP. Join immediately on first victorious boss terminal handoff, before loot/reward/reveal; explicit Continue required. Area boss detection uses node slot, independent of area; legacy final Moonlit Master is its sole boss. Preserve starting map locks and pre-boss combat.
Support Strike acts after nonlethal player impact, before foe reply, once per turn, deterministic 1 + mastery + WATER matchup. Ward Pulse instead blocks up to mastery+1 incoming player damage. Surviving enemy replies also deal one fatigue damage to Kira; at zero HP support stops until shrine recovery. No extra RNG or combat input. Shrine development once per visit independently of existing player training, switch ability or upgrade through level 3. Runtime rules exclusively in models; views project state/events. Preserve all old raster hashes.

## Execution record
- Pass 1 complete: Aseprite source/export and independent verifier passed; full gate 20,286 checks, zero failures. Initial Aseprite export needed writable XDG_CONFIG_HOME; rerun succeeded without changing HOME.
- Pass 2 complete: 80 targeted checks, 20,560 full checks, zero failures; Web/pack clears passed. During the gate, fixed a test-driver indentation parse error and map overflow caught by three existing fit assertions. No assertions weakened.
- Pass 3 complete: 96 targeted checks, 20,576 full checks, zero failures; Web/pack resources/clears, deterministic art reproduction, native/independent asset gates and diff check passed. Companion-aware forecasts, portrait and mobile HUD bounds verified. Corrected the native smoke driver's training selection so it cannot stall on a mastered technique.
- Fourth pass unused. No subagents, push or deployment. Local modifications remain uncommitted on e71a0a2 (.git is read-only).
- Native 390×844 attempt failed before rendering: X11 unavailable and Wayland connection failed. Strip inspected directly; no game screenshot/browser/device visual acceptance claimed.
